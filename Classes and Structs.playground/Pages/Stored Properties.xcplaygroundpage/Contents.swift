//: [Previous](@previous)

/*:
 # Stored Properties
 
    `x` and `y` are both stored properties of `Point`. Space in the struct is reserved to hold each member.
 */

struct Point {
    var x: Double
    var y: Double
}

//: [Next](@next)
